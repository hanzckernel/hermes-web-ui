# Technical Appendix: Agent Index

This appendix keeps a compact machine-readable orientation map for maintainers and agents. It is not part of the main user manual navigation. Use `assets/wiki-index.json` for structured metadata.

## Maintenance notes

- Keep user-facing workflow pages in the sidebar.
- Keep implementation details out of visible feature pages unless they affect user behavior.
- Validate internal links before publishing.
